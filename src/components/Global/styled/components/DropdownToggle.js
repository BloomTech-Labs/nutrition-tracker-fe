import styled from "styled-components";
import { DropdownToggle as StrapDropDown } from "reactstrap";

const DropdownToggle = styled(StrapDropDown)`
  font-size: 1.6rem;
  height: 38px;
`;

export default DropdownToggle;
