import React from "react";
import styled from "styled-components";

const Auth = () => {
  return (
    <AuthWrapper>
      <h1>Hello, world!</h1>
    </AuthWrapper>
  );
};

const AuthWrapper = styled.div`
  h1 {
    margin-top: 0;
  }
`;

export default Auth;
